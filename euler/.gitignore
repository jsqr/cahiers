# ===================================================================
# PROJECT-LEVEL .GITIGNORE (JULIA)
# Purpose: Ignore files specific to Julia projects.
# ===================================================================

# NOTE: We do NOT ignore Manifest.toml in a reproducible project.
# Committing Manifest.toml ensures the environment can be perfectly
# recreated. Only ignore it if you are developing a library.

# Pluto notebook static exports
/build/
/public/
/_site/

# Julia's pre-compiled module files
*.ji

# Compiled binary files (if using PackageCompiler.jl or C-interop)
*.so
*.o
*.dylib

# Log files
*.log
